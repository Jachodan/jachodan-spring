<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jaegokeeper.hwan.mapper.ItemMapper">

    <insert id="insertItem"
            parameterType="com.jaegokeeper.hwan.domain.Item"
            useGeneratedKeys="true"
            keyProperty="itemId">

        INSERT INTO item (
            store_id,
            item_name,
            created_at,
            del_yn
        )
        VALUES (
                   #{storeId},
                   #{itemName},
                   NOW(),
                   0
               )
    </insert>

    <select id="countItemList" resultType="long">
        SELECT COUNT(*)
        FROM item i
        JOIN stock s
        ON s.item_id = i.item_id
        AND s.store_id = i.store_id

        <where>
            i.store_id = #{storeId}

            <if test="filters != null and filters.contains('FAVORITE')">
                AND s.favorite_yn = 1
            </if>
            <if test="filters != null and filters.contains('ZERO_STOCK')">
                AND s.quantity = 0
            </if>
            <if test="keyword != null and keyword != ''">
                AND i.item_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <select id="findItemList"
            parameterType="map"
            resultType="com.jaegokeeper.hwan.dto.ItemListDTO">
        SELECT
        i.item_id   AS itemId,
        i.item_name AS itemName,
        s.quantity  AS quantity,
        s.favorite_yn AS favoriteYn,
        NULL        AS imageUrl
        FROM item i
        JOIN stock s
        ON s.item_id = i.item_id
        AND s.store_id = i.store_id

        <where>
            i.store_id = #{storeId}

            <if test="filters != null and filters.contains('FAVORITE')">
                AND s.favorite_yn = 1
            </if>
            <if test="filters != null and filters.contains('ZERO_STOCK')">
                AND s.quantity = 0
            </if>
            <if test="keyword != null and keyword != ''">
                AND i.item_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>

        ORDER BY i.item_id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="findItemDetail"
            resultType="com.jaegokeeper.hwan.dto.ItemDetailDTO">

        SELECT
            i.item_id   AS itemId,
            i.item_name AS itemName,
            s.quantity  AS quantity,
            s.favorite_yn AS favoriteYn,
            NULL        AS imageUrl,
            sa.safe_quantity AS safeQuantity,
            GREATEST(i.updated_at, s.updated_at) AS lastUpdatedAt
        FROM item i
                 JOIN stock s
                      ON s.item_id = i.item_id
                          AND s.store_id = i.store_id
                 LEFT JOIN safe sa
                           ON sa.stock_id = s.stock_id
        WHERE i.store_id = #{storeId}
          AND i.item_id  = #{itemId}
          AND i.del_yn   = b'0'
    </select>

</mapper>
