<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jaegokeeper.hwan.mapper.StockMapper">

    <insert id="insertStock"
            parameterType="com.jaegokeeper.hwan.domain.Stock"
            useGeneratedKeys="true"
            keyProperty="stockId">

        INSERT INTO stock (
            item_id,
            store_id,
            quantity,
            favorite_yn
        )
        VALUES (
                   #{itemId},
                   #{storeId},
                   #{quantity},
                   0
               )
    </insert>

    <select id="findStockKey" resultType="com.jaegokeeper.hwan.dto.StockKeyDTO">
        SELECT stock_id as stockId,
               quantity as quantity
        FROM stock
        WHERE store_id = #{storeId}
        AND   item_id = #{itemId}
    </select>

    <update id="updateQuantity">
            UPDATE stock
            SET quantity = #{quantity},
                favorite_yn = #{favoriteYn}
            WHERE stock_id = #{stockId}
    </update>


</mapper>
